# web-api

## 职责
将基础的爬虫能力封装为 HTTP API 形式，为 `web-ui` 或第三方客户端提供调度服务。

## 文件
| File | Purpose |
|------|---------|

## 依赖
- fastapi
- uvicorn

## 使用
```bash
# 启动服务
python src/modules/web-api/main.py
```
